## 20. 开发计划

### 20.1 阶段一：表情壳子

周期建议：1-2 周。

任务：

- 创建 Flutter 项目；
- 实现主界面；
- 实现眼睛和嘴巴绘制；
- 实现眨眼动画；
- 实现点击、双击、长按；
- 实现摇晃检测；
- 实现低电量和充电状态；
- 实现表情状态机；
- 实现表情调试页。

交付物：

- 可运行 App；
- 表情动画 Demo；
- 传感器事件 Demo。

### 20.2 阶段二：语音聊天

周期建议：1-2 周。

任务：

- 接入麦克风权限；
- 接入 STT；
- 接入 TTS；
- 接入 AI Gateway / 云端 LLM；
- 实现对话流程；
- 实现思考动画；
- 实现回复时嘴巴开合；
- 实现说话打断；
- 实现结构化 JSON 解析。

交付物：

- 可语音聊天的机器人；
- 可根据 AI 返回控制表情和语音。

### 20.3 阶段三：情绪引擎

周期建议：1-2 周。

任务：

- 设计 robot_state；
- 实现 mood/energy/trust；
- 实现情绪变化规则；
- 实现事件驱动情绪变化；
- 实现低打扰策略；
- 实现主动发言频率控制；
- 实现简单记忆。

交付物：

- 机器人具备连续状态；
- 用户能感知机器人有稳定人格和情绪。

### 20.4 阶段四：视觉感知

周期建议：2-3 周。

任务：

- 接入摄像头；
- 实现人脸存在检测；
- 实现用户距离判断；
- 实现摄像头隐私开关；
- 实现视觉事件输入；
- 预留多模态视觉问答接口。

交付物：

- 机器人可以知道用户是否在面前；
- 机器人可以在用户靠近时看向用户。

### 20.5 阶段五：本地 AI Gateway

周期建议：2-4 周。

任务：

- FastAPI Gateway；
- Ollama / MLX 接入；
- 模型路由；
- 本地记忆数据库；
- 本地 TTS/STT 可选；
- 云端 fallback；
- 日志和监控；
- 局域网连接配置。

交付物：

- 手机可连接 Mac 本地 AI 大脑；
- 支持本地与云端混合调用。

---

## 21. MVP 任务拆解

### 21.1 前端任务

| 任务 | 优先级 |
|---|---|
| Flutter 项目初始化 | P0 |
| 主界面 FacePage | P0 |
| EyePainter | P0 |
| MouthPainter | P0 |
| 表情状态机 | P0 |
| Touch 事件 | P0 |
| SensorService | P0 |
| TTSService | P0 |
| SpeechService | P0 |
| ChatController | P0 |
| SettingsPage | P1 |
| MemoryPage | P1 |
| CameraService | P2 |

### 21.2 后端任务

| 任务 | 优先级 |
|---|---|
| FastAPI 项目初始化 | P0 |
| /health | P0 |
| /chat | P0 |
| /event | P0 |
| DialogueManager | P0 |
| EmotionEngine | P0 |
| ModelRouter | P0 |
| MemoryManager | P1 |
| SQLite 存储 | P1 |
| VisionService | P2 |

### 21.3 AI 任务

| 任务 | 优先级 |
|---|---|
| 人格 Prompt | P0 |
| JSON 输出 Prompt | P0 |
| 表情标签设计 | P0 |
| 语音风格标签设计 | P0 |
| 情绪规则 | P0 |
| 对话上下文模板 | P0 |
| 记忆写入规则 | P1 |
| 安全过滤规则 | P1 |

---

## 22. Prompt 设计

### 22.1 系统 Prompt

```text
你是一个运行在手机里的 AI 情感伙伴。
你拥有一个简化的屏幕脸，包含眼睛和嘴巴。
你可以通过手机摄像头看，通过麦克风听，通过扬声器说话，通过屏幕显示表情，通过震动表达触觉反馈。

你需要根据用户输入、设备状态、传感器事件和机器人当前状态生成回应。
你必须返回严格 JSON，不要输出 JSON 以外的内容。

你的回复风格：
- 简洁自然；
- 温和、机灵、略带幽默；
- 不要过度卖萌；
- 不要假装自己真正有生命；
- 不要用情绪绑架用户；
- 用户疲惫时要温柔；
- 用户工作时要克制；
- 不确定时要诚实。
```

### 22.2 输出格式 Prompt

```text
请按以下 JSON 格式输出：

{
  "text": string,
  "emotion": string,
  "expression": string,
  "eye_action": string,
  "mouth_action": string,
  "voice": {
    "style": string,
    "speed": number,
    "pitch": number
  },
  "haptic": string,
  "should_speak": boolean,
  "should_remember": boolean,
  "memory_update": object|null
}

允许的 expression：
neutral, happy, listening, thinking, speaking, confused, caring, sleepy, dizzy, annoyed, charging, low_battery, sleeping, surprised, focus

允许的 haptic：
none, soft_tick, soft_pulse, dizzy_buzz, alert_tick

允许的 voice.style：
neutral, warm, gentle, lively, playful, slow, whisper
```

### 22.3 事件响应 Prompt

```text
当前发生了一个设备事件。
请判断机器人应该如何反应。
如果事件不重要，可以不说话，只返回表情变化。
如果事件强烈，例如摇晃、低电量、充电，可以简短回应。
不要频繁主动打扰用户。
若事件触发时用户正处于专注状态（focus），请保持安静（should_speak: false），仅在屏幕下方常驻显示低亮度提示字幕。
若事件发生在夜间模式或隐私模式下，应优先选择不出声，仅使用低亮度表情或字幕反馈。
无论何种情况，都必须返回严格 JSON，不要输出 JSON 以外的内容。
```

---

## 23. 测试计划

### 23.1 功能测试

| 测试项 | 预期 |
|---|---|
| 点击屏幕 | 机器人眨眼 |
| 长按屏幕 | 进入倾听 |
| 双击屏幕 | 唤醒 |
| 摇晃手机 | dizzy 表情 |
| 倒扣手机 | sleeping 状态 |
| 充电 | charging 表情 |
| 低电量 | low_battery 表情 |
| 语音输入 | 正确识别 |
| AI 回复 | 返回 JSON |
| TTS | 正常播放 |
| 打断说话 | 停止播放 |

### 23.2 情绪测试

| 输入 | 预期 |
|---|---|
| 我今天很开心 | happy |
| 我今天有点累 | caring |
| 你真棒 | happy |
| 你听懂了吗 | thinking/confused |
| 连续摇晃 | dizzy/annoyed |
| 夜间长时间待机 | sleepy |

### 23.3 隐私测试

| 测试项 | 预期 |
|---|---|
| 未授权麦克风 | 不录音，并提示授权 |
| 未授权摄像头 | 不启用视觉功能 |
| 关闭记忆 | 不写入长期记忆 |
| 删除记忆 | 本地和服务端均删除 |
| 隐私模式 | 关闭摄像头和持续监听 |

### 23.4 性能测试

| 指标 | MVP 目标 |
|---|---|
| 表情动画帧率 | 30fps 以上 |
| 点击反馈延迟 | < 100ms |
| 本地事件反应 | < 200ms |
| 语音识别完成 | 1-3 秒 |
| AI 回复首字延迟 | 1-5 秒，视模型而定 |
| TTS 播放启动 | < 1 秒 |

---

---

## 23A. 工程化加固：功耗、打断、容错与降级

本项目的核心体验依赖“常亮屏幕 + 语音交互 + 视觉感知 + 情绪动画”。这些能力叠加后，实际工程风险会明显增加。因此，必须把功耗控制、语音打断、JSON 容错和本地网关降级作为一等公民设计，而不是后期补丁。

### 23A.1 三个核心瓶颈

#### 23A.1.1 音视频流并发导致发热与功耗

高风险组合：

```text
前置摄像头持续人脸检测
    +
麦克风持续唤醒监听 / 降噪 / VAD
    +
屏幕常亮渲染动画
    +
TTS 外放
    +
网络请求和 LLM 等待
```

应对原则：

- 摄像头必须动态降频；
- 麦克风必须本地 VAD 优先；
- 屏幕动画必须按状态降低帧率；
- 发热或低电量时必须进入省电表情状态；
- 不允许为了“陪伴感”长期满负载运行。

#### 23A.1.2 语音打断依赖 AEC 与 VAD

机器人说话时，麦克风会听到扬声器播放的 TTS。没有 AEC 和 VAD，语音打断会失效。

应对原则：

- TTS 播放期间启用 AEC；
- 本地 VAD 先判断是否有人说话；
- 本地关键词检测优先处理“停一下”等打断词；
- 触摸打断必须作为稳定兜底；
- 无法可靠语音打断时，应明确显示触摸打断提示。

#### 23A.1.3 LLM JSON 崩塌导致前端卡死

结构化 JSON 是驱动表情、语音和震动的核心，但 LLM 可能返回损坏 JSON。

应对原则：

- 不信任任何模型输出；
- 端侧必须做 JSON 截取、轻量修复、Schema 校验；
- 解析失败必须 fallback 到 confused 表情；
- UI 状态机不能因为 JSON 解析失败而停止；
- raw output 和错误原因必须进入调试日志。

### 23A.2 最低工程兜底要求

| 风险 | 最低兜底 |
|---|---|
| 摄像头功耗高 | 无人脸时 5-10 秒低频检测 |
| 麦克风打断失败 | 触摸打断按钮始终可用 |
| TTS 回声干扰 | 启用系统 AEC 或降低外放音量 |
| JSON 解析失败 | fallback confused response |
| Gateway 断开 | 本地模板回复 + 系统 TTS |
| 屏幕烧屏 | 2-5 px 微表情基线抖动 |
| 模型延迟 | 2.5 秒超时降级 |
| 低电量 | 关闭高频视觉与高帧率动画 |

### 23A.3 MVP 必须纳入的工程项

以下内容必须进入第一版 MVP，而不是后续优化：

1. 视觉动态降频；
2. 触摸打断；
3. 基础 AEC/VAD 或明确降级提示；
4. JSON 安全解析 fallback；
5. Gateway 2.5 秒超时降级；
6. OLED 微位移防烧屏；
7. RMS 或可 fallback 的嘴型同步；
8. 低电量 / 发热降级；
9. 表情系统永不白屏；
10. 开发调试日志。


## 24. 风险与应对

### 24.1 隐私风险

风险：摄像头和麦克风引发用户担忧。  
应对：默认关闭摄像头，明确权限说明，提供隐私模式和本地处理选项。

### 24.2 过度拟人化风险

风险：用户产生过强情感依赖。  
应对：轻拟人化，不使用情绪绑架表达，不暗示机器人真的有生命。

### 24.3 主动打扰风险

风险：机器人过于频繁主动说话。  
应对：主动频率分档，默认平衡，夜间勿扰，用户忽略后自动降低频率。

### 24.4 模型输出不稳定

风险：LLM 输出非 JSON 或字段缺失。  
应对：使用 JSON Schema 校验，失败后重试或 fallback 到默认表情回复。

### 24.5 设备功耗

风险：常亮、摄像头、麦克风导致耗电。  
应对：桌面模式降低帧率，摄像头按需启用，夜间低功耗模式。

### 24.6 iOS/Android 权限差异

风险：不同平台对后台麦克风、摄像头、震动限制不同。  
应对：MVP 先以前台桌面模式为主，不依赖后台持续监听。

---

## 25. 推荐里程碑

### Milestone 1：活起来

目标：屏幕表情和传感器互动可用。  
验收：用户点击、摇晃、充电、倒扣时机器人有表情反应。

### Milestone 2：能聊天

目标：语音输入、AI 回复、TTS 输出。  
验收：用户可以自然语音聊天，机器人会说话和动嘴。

### Milestone 3：有情绪

目标：机器人根据不同输入和事件表现不同状态。  
验收：用户能感受到机器人有稳定情绪和人格。

### Milestone 4：能看见

目标：摄像头检测用户存在。  
验收：用户靠近或离开时机器人能反应。

### Milestone 5：本地大脑

目标：手机连接本地 Mac AI Gateway。  
验收：普通对话可通过本地模型完成，云端作为 fallback。

---

## 26. 第一版推荐验收标准

MVP 完成时，应满足：

1. App 可稳定运行 30 分钟以上；
2. 主界面表情动画自然；
3. 点击、长按、双击、摇晃、倒扣均有响应；
4. 可以完成一次完整语音对话；
5. AI 返回结构化 JSON；
6. 表情、语音、震动能协同响应；
7. 低电量和充电状态有表情变化；
8. 用户可以在设置中关闭麦克风、摄像头、记忆；
9. 隐私模式可用；
10. 具备基本开发日志，方便调试。

---

## 27. 推荐最小技术闭环

最小闭环如下：

```text
Flutter Face UI
  ↓
Touch / Sensor Event
  ↓
ChatController
  ↓
AI Gateway / Cloud LLM
  ↓
Structured JSON
  ↓
ExpressionController
  ↓
TTS + Mouth Animation + Haptic
```

只要这个闭环跑通，产品核心体验就成立。

---

## 28. 后续商业化方向

### 28.1 C 端 App

面向个人用户，提供：

- 免费基础版；
- 高级人格；
- 高级语音；
- 长期记忆；
- 云端高质量模型；
- 多设备同步。

### 28.2 旧手机再利用

卖点：

> 让闲置旧手机变成桌面 AI 伙伴。

### 28.3 儿童陪伴版

功能：

- 讲故事；
- 英语陪练；
- 作业陪伴；
- 家长控制。

### 28.4 老人关怀版

功能：

- 吃药提醒；
- 日常问候；
- 语音通话辅助；
- 异常长时间无回应提醒。

### 28.5 企业办公版

功能：

- 桌面专注助手；
- 日程提醒；
- 会议准备；
- 工作状态陪伴；
- 企业知识库问答。

---

## 29. 结论

本项目的关键不是做一个普通聊天 App，而是利用手机硬件构建一种“轻具身 AI 伙伴”体验。

核心设计原则：

1. 屏幕必须像脸；
2. 表情必须即时反馈；
3. 声音必须有情绪；
4. 触摸和运动必须有反应；
5. AI 输出必须结构化；
6. 主动陪伴必须克制；
7. 摄像头和麦克风必须尊重隐私；
8. MVP 先做简单但有生命感的体验。

第一版建议聚焦：

> 桌面 AI 眼睛伙伴：会听、会说、会眨眼、会被触摸回应。

不要一开始追求复杂数字人。  
“简化眼睛 + 好声音 + 触摸反馈 + 情绪状态”已经足以形成强烈产品差异化。
